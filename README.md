# EX2 Control — atualizações

Este repositório hospeda apenas os artefatos de atualização (`version.json` +
`app-release.apk`) usados pelo mecanismo de auto-atualização de um app de sistema
para o Geely EX2. Não contém código-fonte.
